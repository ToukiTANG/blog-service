<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.touki.blog.mapper.BlogTagDtoMapper">
    <select id="getListByBlogIds" resultType="com.touki.blog.model.dto.BlogTagDto">
        select bt.blog_id, bt.tag_id, t.tag_name
        from blog_tag bt
        join tag t on t.tag_id = bt.tag_id
        where bt.blog_id in
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>
</mapper>